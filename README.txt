ST. JOHN'S LODGE NO. 63 - STATIC WEBSITE UPLOAD PACKAGE

Upload everything inside this folder to your website root:
- index.html
- styles.css
- script.js
- /assets folder

QUICK EDITS
1. Hero image:
   Replace assets/hero-stjohns-night.jpg with a better final photo using the same file name.

2. Officers:
   In index.html, search for "Current officer" and replace the placeholder officer names/photos.

3. Contact form:
   The current form uses mailto:secretary@stjohns63.ca.
   For a real web form, connect it to Wix/WordPress/Formspree/Netlify Forms or your host's form handler.

4. Members Area:
   The Members Login button points to https://www.stjohns63.ca/members-area

NOTES
- This is mobile responsive.
- The header collapses into a mobile menu.
- The design uses the real lodge building photo supplied in the conversation, with a dark cinematic treatment.
- Keep all filenames the same if uploading over an existing static host.
